---
description: Show guard installation status, log location, line count, and last record.
allowed-tools: Bash(guard status)
disable-model-invocation: true
---

# /guard:status

Print guard's effective config (mode, schema version), the JSONL decision-log
path, the line count, and the timestamp of the most recent record.

```!
guard status
```

Display the output above to the user verbatim.
