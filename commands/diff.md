---
description: Show the effective merged config (built-in defaults; user/project layers land later).
allowed-tools: Bash(guard diff)
disable-model-invocation: true
---

# /guard:diff

Print guard's effective merged config. For v1.1 this is a stub showing the
built-in defaults; user and project config layers land in a later release,
at which point this command expands to a 3-way merge view.

```!
guard diff
```

Display the output above to the user verbatim.
