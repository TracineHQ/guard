# Contributing

Open an issue first for non-trivial changes. Run `just check` before opening PRs.

## Pre-commit (optional)

CI runs the same checks. To install local pre-commit hooks: `uv run pre-commit install`.
